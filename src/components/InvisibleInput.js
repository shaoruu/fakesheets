import styled from 'styled-components';

export default styled.input`
  background: transparent;
  border: none;
  width: 100%;
  height: 100%;
  font-size: inherit;
  color: inherit;
  &:focus {
    outline: none;
  }
`;
